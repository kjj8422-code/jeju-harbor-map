# 제주 어항 해도 — 인수인계 문서 (HANDOFF)

이 문서는 Claude 계정 접근이 막히거나, 다른 사람/다른 AI가 이어받아 작업해야 할 때를
위한 자료입니다. **이 사이트는 Claude와 완전히 무관하게 독립적으로 존재합니다** —
GitHub(당신 계정)에 호스팅되어 있고, Claude가 사라져도 사이트는 그대로 살아있습니다.
Claude가 필요한 건 오직 "코드를 수정해서 다시 올리는 작업을 대신 해주는 것"뿐입니다.

## 지금 당장 알아야 할 것

- **라이브 사이트**: https://kjj8422-code.github.io/jeju-harbor-map/
- **소스코드 저장소**: https://github.com/kjj8422-code/jeju-harbor-map
- **로컬 작업 사본**: `C:\Users\KOCOwj\jeju-harbor-map` (임시 폴더 아님, 컴퓨터에 영구 보관됨)
- **원본 실습 파일**: `C:\Users\KOCOwj\실습파일\제주_어항_해도.html` (동일 내용, 처음 만든 위치)

## ⚠ 계정 관리 — 가장 중요

이 프로젝트의 진짜 "키"는 Claude 계정이 아니라 **GitHub 계정(`kjj8422-code`)**입니다.
- 이 계정은 이 세션에서 기기 인증(device code)으로 로그인했습니다. **본인이 이 GitHub
  계정의 로그인 정보(이메일/비밀번호 또는 2단계 인증)를 직접 확보하고 계신지 꼭 확인하세요.**
  모르시면 지금 github.com에서 "비밀번호 찾기"로 복구해 두시길 권합니다.
- GitHub 계정만 있으면, Claude 없이도 GitHub 웹사이트에서 직접 코드를 고치고
  게시할 수 있습니다 (아래 "방법 B" 참고).

## 사이트 구조

```
jeju-harbor-map/
├── index.html          ← 전체 사이트 (지도+표+계산기 전부 이 한 파일 안에 있음)
├── manifest.json        ← PWA 설정(홈 화면 추가용)
├── sw.js                 ← 오프라인 캐시용 서비스워커
├── og-image.png          ← 카카오톡 공유 미리보기 이미지
├── robots.txt / sitemap.xml  ← 검색엔진용
├── icons/                ← 앱 아이콘들
├── docs/
│   └── underwater-leisure-cert-orgs-2026-05.pdf  ← 해수부 원본 자료(안전관리요원 인정단체)
└── HANDOFF.md            ← 이 문서
```

`index.html` 하나가 전부입니다 (순수 HTML/CSS/JS, 빌드 과정 없음 — 그냥 파일을 고치고
그대로 올리면 끝). 안에 4개 섹션 + 3개의 핵심 JS 데이터 배열이 있습니다:

| 섹션 | 내용 | index.html 안 데이터 배열 위치(대략 줄) |
|---|---|---|
| ① 어항구역 지도 | 제주 어항 69곳 위치·검색 | `const DATA = [...]` |
| ② 금어기·금지체장 | 수산물 52종 규정 | `const RULES = [...]` |
| ③ 오늘의 물때·날씨 | 물때 자동계산(수식) + 실시간 링크 + Windy 바람·파도 지도 + CCTV | 계산 로직만 있고 데이터 배열 없음 |
| ④ 야간 해루질 안전기준 | 안전관리요원 인정단체 50곳 | `const ORGS = [...]` |
| ⑤ 조과자랑 게시판 | 구글 폼+시트 연동 게시판 (설정 필요) | `GOOGLE_FORM_URL` / `GOOGLE_FORM_EMBED_URL` / `SHEET_CSV_URL` |

줄 번호는 수정하면서 계속 바뀌니, 텍스트 에디터에서 `const DATA = [` 같은 문자열로
검색해서 찾는 게 제일 확실합니다.

## 데이터 출처 (재확인이 필요할 수 있는 것들)

아래는 전부 "이 시점 기준" 자료입니다. 시간이 지나면 바뀔 수 있으니, 큰 결정을 내리기 전엔
다시 한번 확인하는 게 안전합니다.

| 데이터 | 근거 | 기준 시점 | 재확인 채널 |
|---|---|---|---|
| 항·포구 물놀이 금지법 | 어촌·어항법 개정 | 2027.4.22 시행 예정 (2026.5월 보도 기준, 최종 대상 포구 목록은 그때까지 미확정이었음) | 제주도청 064-710-2114 |
| 어항 69곳 목록/주소 | 한국어촌어항공단 공개자료 | 이 세션 조사 시점 | fipa.or.kr |
| 어촌정주어항 46곳(전체) | 한국어촌어항공단 공식 사이트 실시간 재조회 | 2026.9.7 확인 (전체 46건, "48곳" 표기는 오류였음) | fipa.or.kr → 정보공개 → 정주어항 현황 |
| 금어기·금지체장 | 해수부 리플렛 | 2024.4 발표분 | law.go.kr "수산자원관리법 시행령" 별표2 |
| 소라/전복/오분자기/참문어/넓미역 제주 특례 | 제주도 조례 + 언론보도 | 2026.5월 기준 | 제주도청 해양수산국 |
| 안전관리요원 인정단체 50곳 | 해수부 공식 PDF (당신이 직접 전달) | **2026.5월 기준** — `docs/underwater-leisure-cert-orgs-2026-05.pdf` 원본 보관 중 | 해당 단체 직접 문의 |

## 수정하고 다시 게시하는 방법

### 방법 A — 컴퓨터에서 (Claude 없이, git 사용)

```bash
cd C:\Users\KOCOwj\jeju-harbor-map
# index.html 등을 원하는 대로 수정
git add -A
git commit -m "수정 내용 설명"
git push
```
`git push`가 성공하면 1~2분 안에 https://kjj8422-code.github.io/jeju-harbor-map/ 에 자동 반영됩니다.
(이 컴퓨터에는 이미 git과 GitHub CLI(`gh`)가 설치·로그인되어 있습니다.)

### 방법 B — 브라우저만으로 (아무 도구도 필요 없음)

1. https://github.com/kjj8422-code/jeju-harbor-map 접속 (GitHub 계정 로그인)
2. `index.html` 클릭 → 연필 아이콘(Edit) 클릭
3. 내용 수정 → 하단 "Commit changes" 클릭
4. 1~2분 뒤 사이트에 자동 반영

### 방법 C — 다른 AI(ChatGPT, 다른 Claude 계정 등)에게 이어서 시키기

이 저장소 링크와 이 HANDOFF.md를 보여주고 이렇게 요청하면 됩니다:

> "https://github.com/kjj8422-code/jeju-harbor-map 이 저장소의 index.html을 수정해줘.
> HANDOFF.md에 구조 설명이 있어. [원하는 수정 내용]을 반영하고, git으로 커밋·푸시까지 해줘."

코드 자체가 평범한 HTML/CSS/JS라 어떤 AI 코딩 도구든 바로 이해하고 이어서 작업할 수 있습니다.

## 지금까지 완료된 것

- [x] 어항구역 지도(69곳) + 검색/필터 + 네이버지도·카카오맵 바로가기
- [x] 금어기·금지체장 52종 표(제주 전용 기준 5개 포함)
- [x] 물때(몇물) 자동 계산기 + 날씨·파고·조위 실시간 링크
- [x] 실시간 CCTV 링크(공식 지자체 소스) — 재난안전대책본부·서귀포시청에 더해 **제주 교통정보센터
      해안도로 CCTV(276대)**, **한라산 실시간 CCTV** 추가 (2026-09-07)
- [x] **실시간 바람·파도·너울 상세 지도(Windy 공식 임베드 위젯)** 추가 — "오늘의 물때·날씨" 섹션
      (2026-09-07)
- [x] 야간 해루질 안전관리요원 기준 + 인정단체 50곳 검색 표
- [x] GitHub Pages 배포, 로그인 없이 누구나 접속 가능
- [x] PWA(홈 화면 설치, 오프라인 캐시)
- [x] 카카오톡 공유 미리보기 카드(OG 이미지)
- [x] SEO 기본 세팅(robots.txt, sitemap.xml, 구조화 데이터)
- [x] GitHub 내장 방문자 통계 (별도 가입 불필요, 저장소 Insights > Traffic 또는 `gh api repos/kjj8422-code/jeju-harbor-map/traffic/views`)
- [x] **정주어항 개수 정정** — 한국어촌어항공단 공식 사이트(fipa.or.kr)를 2026-09-07에 직접
      재조회한 결과, 제주 어촌정주어항은 **46곳이 전체**임을 확인 (이전에 알려졌던 "48곳"은
      갱신 전 자료로 보임). 지도에 이미 46곳이 다 들어있어 추가할 항목은 없었고, 안내 문구만
      정정함 (`index.html`의 "개략도 안내" 문단).
      확인 방법: fipa.or.kr → 정보공개 → 어항정보 → 정주어항 현황 → 목록보기 →
      도/광역시에서 "제주도" 선택 → 전체 46건.
- [x] **광고(Google AdSense) 삽입 자리 마련** — `index.html`에 `#adSlotTop`(상단), `#adSlotBottom`
      (게시판 하단) 두 자리를 만들어둠. 광고 코드가 없으면 자동으로 숨겨져 빈 공간이 안 보임.
      승인받은 애드센스 코드를 해당 주석 자리에 붙여넣기만 하면 바로 노출됨 (2026-09-07).
- [x] **조과자랑 게시판 틀(스캐폴드)** — 구글 폼+시트 연동 UI/로직을 전부 만들어둠
      (`#board` 섹션, `GOOGLE_FORM_URL`/`GOOGLE_FORM_EMBED_URL`/`SHEET_CSV_URL` 세 값만 채우면
      작동). XSS 방지를 위해 시트에서 불러온 텍스트는 전부 이스케이프 처리함. 아래 "조과자랑
      게시판 만들기" 항목의 구글 폼 생성 자체는 사용자 본인 구글 계정으로 직접 해야 함.
- [x] **메인 화면을 탭 방식으로 전면 개편** (2026-09-07) — 예전엔 모든 섹션이 세로로 길게
      나열되어 있었는데, 이제 상단 ①~⑦ 버튼을 눌러야 그 섹션만 바로 아래에 나타남(`.tab-panel`
      + `#tabNav` 클릭 이벤트, `initTabs()` 함수). 새 섹션 추가 시 이 패턴을 따를 것: `<section
      class="tab-panel" hidden>`로 만들고 `#tabNav`에 버튼 하나 추가.
- [x] **추천장비 탭(스킨/워킹 해루질) + 쿠팡 제휴 링크 틀** (2026-09-07) — `#gear` 섹션,
      `GEAR_SKIN`/`GEAR_WALKING` 배열(각 상품의 이름·설명·검색어)과 `renderGear()` 함수로
      카드 자동 생성. 지금은 전부 쿠팡 "일반 검색 링크"(수익 없음)로 연결되어 있음 — 아래
      "추천장비 제휴 링크 연결하기" 항목 참고. 비어업인 금지 장비(작살·스쿠버 공기통·집어등)
      안내박스도 함께 추가함(수산자원관리법 시행규칙 제6조/별표1 근거).

## 🤖 법령 자동 감시 (2026-09-07 구축, PC를 꺼도 GitHub 서버에서 자동 실행됨)

**동작 방식**
- 매일 아침 06:00(KST) `.github/workflows/law-watch.yml`이 자동 실행됩니다.
- `law-watch/sources.json`에 적힌 법령·고시 페이지 6곳을 가져와, 어제 저장해둔 지문(해시)과 비교합니다.
- 달라졌으면 **이 저장소에 이슈를 자동으로 만들고**(라벨 `law-watch`), 어떤 문구가 생기고 없어졌는지까지 적어줍니다.
  → 저장소 주인에게 GitHub 알림 메일이 갑니다.
- 3일 연속 수집에 실패하면(기관 홈페이지 주소 변경 등) 그것도 이슈로 알려줍니다.
- 매월 1일에는 `monthly-review.yml`이 사람이 직접 볼 체크리스트 이슈를 만듭니다.

**오탐(잘못된 알림) 방지**
- 오늘 날짜, 세션 토큰, 게시판 글 개수(`행정 (4886)`) 같은 매일 바뀌는 값은 지운 뒤 비교합니다.
- 보도자료 목록처럼 매일 새 글이 올라오는 곳은 `keywords`에 적힌 단어(금어기·소라·해루질 등)가
  들어간 줄만 감시합니다.
- 구축 시 2회 연속 실행해 오탐 0건을 확인했습니다.

**중요한 한계 (반드시 알고 있을 것)**
- 이 시스템은 **"바뀐 것 같으니 사람이 확인해라"고 알려주는 역할만** 합니다.
  법 해석과 사이트 반영은 반드시 사람이 확인 후 해야 합니다. (법령은 자동 반영하면 위험)
- 금어기 별표2 원문은 자바스크립트로 그려져 자동 수집이 불가능해, **모법인 시행령 본문**을
  감시합니다. 별표는 시행령의 일부라 별표가 바뀌면 시행령 시행일자·대통령령 호수가 바뀌므로
  변경 자체는 반드시 잡힙니다(다만 "어느 어종이 바뀌었는지"는 사람이 원문에서 확인해야 함).
- GitHub은 **60일 이상 아무 활동이 없는 저장소의 예약 작업을 자동 중지**합니다.
  두 달 넘게 손대지 않았다면 Actions 탭에서 다시 켜주세요.

**직접 지금 돌려보고 싶을 때**: GitHub 저장소 → Actions 탭 → "법령 자동 감시" → Run workflow

**감시 대상을 추가/수정하려면**: `law-watch/sources.json` 편집 (id는 파일명으로 쓰이니 영문으로)

## 📌 법령 변경 이력 (사이트에 표시되는 부분)

`index.html`의 `LAW_CHANGES` 배열에 항목을 추가하면 ① 금어기 탭 상단의 "최근 법령 변경 이력"
박스에 자동으로 표시되고, `species`에 적은 품종은 표에서 🆕 뱃지가 붙습니다.

```js
{
  effective: "2026-07-01",     // 시행일 (이 날짜 기준 자동 정렬)
  status: "시행중",             // "시행중" 또는 "시행 예정"
  scope: "금어기·금지체장",
  title: "무엇이 바뀌었는지 한 줄",
  before: "이전에는 이랬음",
  after: "지금은 이렇게 바뀜",
  note: "부연설명(HTML 태그 사용 가능)",
  source: "근거 이름",
  sourceUrl: "https://...",
  species: ["고등어", "갈치"],  // 표에 🆕 뱃지를 붙일 품종(선택)
}
```

## 💰 수익화 현황과 다음 단계

| 수단 | 상태 | 다음 할 일 |
|---|---|---|
| 쿠팡 파트너스(제휴) | **작동 중** — 8개 상품 연결됨 | 나머지 "준비중" 항목 링크 추가 |
| 구글 애드센스 | 자리만 준비됨(코드 없음) | 애드센스 가입·승인 → `index.html` head 주석 자리에 코드 한 줄 |
| ads.txt | 준비됨 | 승인 후 한 줄 입력 |
| 개인정보처리방침 | **완료**(privacy.html) | 필요시 문의 이메일 추가 |

**애드센스 승인 팁(중요)**: `kjj8422-code.github.io`는 서브도메인이라 승인이 까다로울 수 있습니다.
승인률과 광고 단가를 함께 높이려면 **자체 도메인(연 1~2만원)** 구입 후 GitHub Pages에 연결하는 것을
권장합니다. 수익 기여도는 일반적으로 **제휴(쿠팡) > 애드센스** 순이므로, 제휴 링크를 채우는 것이
우선순위가 더 높습니다.

## 아직 안 된 것 (다음에 할 일)

1. **조과자랑 게시판 — 구글 폼 실제로 만들기** (코드는 이미 준비됨, 폼 생성만 남음)
   1. forms.google.com 접속 → 새 폼 만들기
   2. 질문 4개 추가: `닉네임`(단답형), `장소·날짜`(단답형), `조과 내용`(장문형),
      `사진 링크(URL)`(단답형, 안내문에 "사진을 구글 포토/네이버 등에 올리고 공유 링크를
      붙여넣어주세요" 추가) — **파일 업로드 항목은 쓰지 말 것** (응답자에게 구글 로그인을
      강제하게 됨)
   3. 폼 우측 상단 "응답" 탭 → 초록색 시트 아이콘 클릭 → 새 스프레드시트 만들기
   4. 그 스프레드시트에서 파일 → 공유 → 웹에 게시 → 시트 선택 → 쉼표로 구분된 값(.csv) →
      게시 → 나오는 URL을 복사 (이게 `SHEET_CSV_URL`)
   5. 폼 우측 상단 점 3개 메뉴 → "미리보기"에서 나오는 주소가 `GOOGLE_FORM_URL`,
      그 주소 끝에 물음표 유무에 따라 `?embedded=true`(또는 `&embedded=true`)를 붙인 게
      `GOOGLE_FORM_EMBED_URL`
   6. `index.html`에서 `GOOGLE_FORM_URL`, `GOOGLE_FORM_EMBED_URL`, `SHEET_CSV_URL` 세 변수에
      위 값을 채우고 git push
2. **매달 자동 재확인 스케줄** — claude.ai 계정에 GitHub를 연결해야 만들 수 있는데, 아직
   연결이 안 됨(claude.ai/code/routines 에서 GitHub Connect 필요). 연결되면 매달 1일 법령·
   데이터 변경사항을 자동으로 재조사하는 루틴을 만들 수 있음.
3. **구글 애드센스 / 카카오 애드핏 계정 가입** — 계정 가입 자체는 본인이 직접 해야 함(신원·
   계좌 정보 필요). 승인 후 나오는 광고 스니펫만 주면 이미 만들어둔 `#adSlotTop`/`#adSlotBottom`
   자리에 바로 삽입 가능.
6. **추천장비 제휴 링크 연결하기** (코드는 이미 준비됨, 계정 가입 + 링크 교체만 남음)
   1. https://partners.coupang.com 접속 → 쿠팡 계정으로 가입(무료, 정산용 계좌 등록 필요)
   2. 승인되면, 파트너스 사이트에서 원하는 상품을 검색 → "링크 생성" → 나오는 딥링크
      (`https://link.coupang.com/a/...` 형태) 복사
   3. `index.html`에서 `GEAR_SKIN`, `GEAR_WALKING` 배열을 찾아(텍스트 검색 `const GEAR_SKIN`),
      해당 상품 객체에 `q:"검색어"` 대신 `url:"복사한 링크"`를 추가
   4. `renderGear()` 함수의 `href="${coupangSearchUrl(item.q)}"` 부분을
      `href="${item.url || coupangSearchUrl(item.q)}"`로 바꾸면, `url`이 있는 상품은 그
      제휴 링크로, 없는 상품은 기존 검색 링크로 자동 연결됨
   5. git push하면 그 순간부터 해당 상품 구매 시 수수료 발생
   - 참고: 이미 페이지에 "쿠팡 파트너스 활동의 일환으로 수수료를 제공받습니다"라는 필수
     고지 문구를 넣어뒀음(공정위 요건) — 지우지 말 것.
4. ~~정주어항 2곳~~ — 위 "지금까지 완료된 것" 항목에서 확인한 대로, 실제로는 누락된 곳이
   없었음 (해결 완료).
5. ~~CCTV 민간 통합 사이트(cctv.trendworld.kr) 연결~~ — 사용자가 참고 사이트로 요청해
   한 번 추가했으나(개별 항구 페이지 링크 포함), 실제 사용해보니 접속이 계속 안정적이지
   않아(SSL 인증서 오류, 간헐적 403/타임아웃) 사용자가 최종적으로 완전히 제외하기로 결정
   (2026-09-07). 공식 소스(제주 교통정보센터 276대, 한라산 CCTV, 재난안전대책본부,
   서귀포시청)만 CCTV 탭에 유지. 다시 추가해달라는 요청이 없다면 재검토 불필요.

## 이 프로젝트에서 배운 제약사항 (다시 시도할 때 참고)

- Claude Artifact(claude.ai 안의 미리보기)는 외부 API 호출·지도 타일 임베드가 CSP로 막혀
  있어서, 실시간 데이터나 진짜 지도를 넣을 수 없었음 → 그래서 GitHub Pages로 옮김.
- Claude Artifact 링크는 보는 사람도 Claude 로그인이 필요해서 카카오톡 공유에 부적합했음
  → 그래서 GitHub Pages(완전 공개, 로그인 불필요)로 이전.
- 예약 클라우드 작업(routine)은 claude.ai 계정에 별도로 GitHub를 연결해야 저장소에 접근
  가능 (로컬 `gh` 로그인과는 별개).
